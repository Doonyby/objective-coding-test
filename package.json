{
  "name": "objective-coding-test",
  "version": "1.0.0",
  "description": "Coding Test Instructions ========================",
  "main": "index.js",
  "scripts": {
    "server": "nodemon --watch server --exec babel-node -- server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "express": "^4.15.4"
  }
}
